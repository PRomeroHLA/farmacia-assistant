# Copiar a .env o .env.local y ajustar. No subir .env.local a git.
#
# URL base del API del backend. En desarrollo el front suele estar en 5173 (Vite) y el back en 8000.
VITE_API_BASE_URL=http://localhost:8000
